<h3>Add a new list below:<h3>
<%= form_for :list, url: lists_path do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  <p>
  <p>
    <%= f.submit %>
  <p>
<% end %>

<%= link_to 'Back', lists_path %>